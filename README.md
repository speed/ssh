# Centos

```
yum -y install git \
  && git clone https://github.com/speed/ssh.git \
  && sh ssh/centos7x64/auth.ssh
```

# Ubuntu
```
apt-get -y update \
  && apt-get -y install git-core \
  && git clone https://github.com/speed/ssh.git \
  && sh ssh/centos7x64/auth.ssh
```
